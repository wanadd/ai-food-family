/** Bottom nav height + safe area — keep in sync with BottomNavigation styles. */
export const BOTTOM_NAV_OFFSET =
  "calc(4.75rem + env(safe-area-inset-bottom, 0px))";

export const STICKY_ACTION_OFFSET =
  "calc(4.75rem + env(safe-area-inset-bottom, 0px) + 0.25rem)";

export const FLOATING_BACK_OFFSET =
  "calc(4.75rem + env(safe-area-inset-bottom, 0px) + 0.5rem)";
