import { SettingsPlaceholder } from "@/components/settings/SettingsPlaceholder";

export default function SettingsUnitsPage() {
  return (
    <SettingsPlaceholder
      title="Единицы измерения"
      description="Здесь можно будет выбрать граммы или унции, миллилитры или чашки, а также формат порций для рецептов и запасов."
    />
  );
}
