import { CollectionDetailView } from "@/components/recipes/CollectionDetailView";

// Деталь коллекции (Этап 2). Сам компонент оборачивается в MenuSectionLayout.
export default function MenuCollectionDetailPage() {
  return <CollectionDetailView />;
}
