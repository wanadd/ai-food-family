import { redirect } from "next/navigation";

// Скелет маршрута «Сценарии». По продуктовому решению «Сценарии» и
// «Из запасов» НЕ являются отдельными внутренними вкладками — это фильтры
// и подборки внутри Рецептов. Маршрут существует для совместимости и пока
// мягко ведёт на действующий каталог рецептов.
export default function MenuScenariosPage() {
  redirect("/recipes");
}
